/*
    main app structure

    NOTES: 
    - REMEMBER -> react components must start with an uppercase letter! (maybe only on mac? not sure...)
*/

import Routing from '../app/Routing.jsx'

function App() {
    return (
        <>
            <Routing/>
        </>
    )
}

export default App 